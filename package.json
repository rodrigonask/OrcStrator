{
  "name": "orcstrator",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run build -w shared && concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "kill": "npx kill-port 3333 5173",
    "restart": "npm run kill; npm run dev",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w server",
    "test": "npm run build",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9",
    "sharp": "^0.34.5",
    "typescript": "^5.6"
  }
}
